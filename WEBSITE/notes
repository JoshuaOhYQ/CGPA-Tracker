Variable:
declaration-    let x;
assignment-     x = 100;  
print-          console.log(`${x}`);
print type-     console.log(typeof x);
text Content-   document.getElementById("id").textContent = `${x}`;

Arithmetic Operators:
Addition-       x = 1 + 1;
Subtraction-    x = 2 - 1;
Multiplication- x = 2 * 3;
Division-       x = 6 / 3;
Exponentiation- x = 2 ** 3;
Modulus-        x = 7 % 3;

Argumented Assignment Operators:
Addition-       x += 1;    
Subtraction-    x -= 1;
Multiplication- x *= 2;
Division-       x /= 2;
Exponentiation- x **= 3;
Modulus-        x %= 3;
Increment-      x++;
Decrement-      x--;

User Input:
(i) Window Prompt:
Assignment-    let x;
Get Input-    x = window.prompt("Enter value:");
Together-     let x = window.prompt("Enter value:");

(ii) HTML textbox:
let username;

document.getElementById("mySubmit").onclick = function(){
    username = document.getElementById("myText").value;
    document.getElementById("myH1").textContent = `Welcome ${username}`;
}

Type Conversion:
Number-   x = Number("100");   
String-   x = String(100);
Boolean-  x = Boolean(1); 

Constants:
Declaration-    const PI = 3.14159;

Math Objects:
Math.PI-        PI value
Math.E-         Euler's number
Math.round()-   round to whole Number
Math.floor()-   rounds down to whole Number
Math.ceil()-    rounds up to whole Number
Math.trunc()-   remove any decimal portions
Math.pow()-     raise to the power of
Math.sqrt()-    square root
Math.log()-     natural logarithm 
Math.sin()-     Trigo (sin)
Math.cos()-     Trigo (cosine)
Math.tan()-     Trigo (tan)
Math.abs()-     eliminates negative sign (modulus)
Math.sign()-    give the sign of the number
Math.max()-     give the maximum value between several variables
Math.min()-     give the minimum value between several variables
Math.random()-  give a random number between 0 and 1







